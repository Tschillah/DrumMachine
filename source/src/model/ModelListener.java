package model;

import java.util.EventListener;


/**
 * Listener interested in model changes.
 *
 */
public interface ModelListener extends EventListener {

	/**
	 * Sent when the model has changed.
	 * 
	 * @param e model event
	 */
	//void modelChanged(ModelEvent e);
	
}
